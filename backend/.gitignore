.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
*.onnx
*.pth

# Local-trained YOLO weights are noise; the canonical weights in
# data/models/ are committed (see exception below).
*.pt
!data/models/*.pt
