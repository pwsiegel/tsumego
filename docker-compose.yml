services:
  api:
    build: ./backend
    ports:
      - "8001:8001"
    volumes:
      - ./backend/models:/app/models:ro
    restart: unless-stopped

  web:
    build: ./web
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped
