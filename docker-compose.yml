services:
  api:
    build: .
    image: tsumego:local
    ports:
      - "8080:8080"
    volumes:
      # Share state with the host so collections/attempts persist across runs
      # and the same files are visible to local dev (`make api`).
      - ${HOME}/data/go-app:/data
    restart: unless-stopped
